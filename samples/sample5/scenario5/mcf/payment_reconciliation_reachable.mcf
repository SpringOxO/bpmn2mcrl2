% Reachability: the deficient branch, settled end, and settled notice
%   must all be reachable in this bounded finance scenario.
<true* . c_balance_reminder(order_id(1))> true
&&
<true* . payment_settled(order_id(1))> true
&&
<true* . c_payment_settled_notice(order_id(1))> true
